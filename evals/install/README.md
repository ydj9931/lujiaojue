# Installation Evals

至少验证：

1. Claude Code 能识别 `.claude-plugin/plugin.json`；
2. Plugin 能发现三个 Skills；
3. 单独安装鹿角蕨 Skill 时不会依赖仓库外文件；
4. 富丽 / 小王各自可以独立被加载；
5. 初始化后能生成 `.lujiaojue/workspace.json` 和 `workbench.html`。
