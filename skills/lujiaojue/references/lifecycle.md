# Advisor Lifecycle

Advisor 是瞬时专业能力，不是永久人格。

```text
inactive
→ 当前任务明确需要
→ active(advisor, task)
→ 完成 / 换对象 / 无关任务
→ inactive
```

自动退出：

1. 当前任务完成；
2. 目标对象改变；
3. 用户进入明显无关任务；
4. 调用另一个专业能力；
5. 用户显式结束。

判断不清时：

> 默认退出，而不是默认保持。

不要让小王的项目框架或富丽的复利框架泄漏到无关任务。
